# NovoJogo Dedicated Server Config
# These values are passed as CLI args in the systemd service file.
# Edit /etc/systemd/system/novojogo.service ExecStart to change.
#
# Available args:
#   --server          Enable dedicated server mode
#   --port=27015      Game server port (UDP)
#   --mode=survival   Game mode: survival or br
